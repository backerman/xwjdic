.entry
  .clear
  .sidebar
    = result[:kanji]
    U#{result[:unicode_hex]}
    %a{:href => detail_url + result[:kanji_text], :target => "_blank"}
      (XML)
  .main
    .readings
      #{result[:japanese]}
    .senses
      = result[:senses].join("; ")
    - if result[:nanori]
      .readings
        Name readings: #{result[:nanori]}
  .clear
